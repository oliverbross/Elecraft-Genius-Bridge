# State Machines

Current state-machine diagrams are in `docs/architecture/phase-1-architecture.md`.

This directory is reserved for expanded state machines after replay validation:

- PGXL TCP session lifecycle.
- TGXL TCP session lifecycle.
- KPA500 serial reconnect lifecycle.
- KAT500 serial reconnect lifecycle.
- Shared-state command intent lifecycle.
- Fault and degraded-mode behavior.

