# Protocol Assumptions Registry

Every inferred behaviour stays here until validated by source, transcript, or hardware.

| Assumption | Source | Confidence | Validation status | Transcript evidence | TODO |
| --- | --- | --- | --- | --- | --- |
| PGXL direct TCP listens on port `9008`. | Phase 1 AetherSDR source inspection | High | Mock smoke tested | `docs/validation/local-tcp-smoke-test.md` | Validate with current AetherSDR build |
| TGXL direct TCP listens on port `9010`. | Phase 1 AetherSDR source inspection | High | Mock smoke tested | `docs/validation/local-tcp-smoke-test.md` | Validate with current AetherSDR build |
| PGXL/TGXL send server-first `V<version>` greeting. | Phase 1 AetherSDR source inspection and local smoke test | High | Mock smoke tested | `docs/validation/local-tcp-smoke-test.md` | Confirm no version format strictness in real AetherSDR |
| PGXL `info` response fields accepted by AetherSDR include `model`, `serial_num`, and `version`. | AetherSDR transcript and replay test | High | AetherSDR polling validated | `logs/protocol/pgxl-1779018198497-127_0_0_1_55157.log` | Validate richer `firmware`/`capabilities` fields |
| TGXL `info` response fields accepted by AetherSDR include `model`, `serial_num`, `version`, and `one_by_three`. | AetherSDR transcript and replay test | High | AetherSDR polling validated | `logs/protocol/tgxl-1779018197185-127_0_0_1_55149.log` | Validate richer `firmware`/`capabilities` fields |
| Direct PGXL `firmware` and `capabilities` info fields are tolerated by AetherSDR. | Compatibility enrichment, not source-proven | Low | Replay tested only | None yet | Validate with AetherSDR after Phase 5 |
| Direct TGXL `firmware` and `capabilities` info fields are tolerated by AetherSDR. | Compatibility enrichment, not source-proven | Low | Replay tested only | None yet | Validate with AetherSDR after Phase 5 |
| PGXL status can include `connection_state` without breaking AetherSDR parsing. | Inferred from key/value protocol style | Low | Not AetherSDR validated | None yet | Validate with AetherSDR; remove or adapt if UI rejects it |
| TGXL status can include `connection_state` and `fault` without breaking AetherSDR parsing. | Inferred from key/value protocol style | Low | Not AetherSDR validated | None yet | Validate with AetherSDR; remove or adapt if UI rejects it |
| AetherSDR PGXL direct TCP does not require operate/standby commands for MVP. | Phase 1 source observation | Medium | Not AetherSDR validated | None yet | Confirm with UI session |
| TGXL `autotune`, `activate ant=N`, and `tune relay=<0|1|2> move=<+1|-1>` are sufficient observed control commands. | Phase 1 source observation | Medium | Replay tested only | `tests/replay/tgxl-polling-session.txt` | Validate with UI controls |
| AetherSDR accessory applet tray visibility is model-driven, not purely transport-driven. PGXL AMP uses radio-side `amplifier` presence; TGXL TUN can also use direct TGXL presence in the inspected source. | Phase 6 source inspection of `AppletPanel`, `MainWindow`, `RadioModel`, and `TunerModel` | High | Explains PGXL hidden applet; TGXL binary behaviour still needs confirmation | `docs/analysis/aethersdr-applet-activation.md` | Validate target macOS AetherSDR binary and decide PGXL integration path |
| AetherSDR PGXL AMP applet visibility is gated by `RadioModel::amplifierChanged(true)`, which is emitted from radio-side `amplifier <handle>` status with a non-empty non-TGXL `model`. | Phase 6 source inspection | High | Source-validated, not injection-tested | `docs/analysis/aethersdr-applet-activation.md` | Validate native Flex radio configuration or proxy injection |
| A PGXL-like device can create the radio-side amplifier presence required by AetherSDR by connecting to the Flex radio API and sending `amplifier create ip=<egb-ip> port=9008 model=PowerGeniusXL serial_num=<serial> ant=<map>`. | FlexRadio TCP/IP `amplifier` docs and PGXL API doc | High | Implemented as Phase 17 passive registration, not yet live-radio validated | None yet | Run `config.flex-injection-readonly.yaml`, capture Flex API stream, and confirm AetherSDR receives `amplifier <handle>` status |
| AetherSDR TGXL TUN applet visibility can be triggered by direct TGXL TCP connection in the inspected source through `TunerModel::m_directPresence`. | Phase 6 source inspection | High | Source-validated, binary behaviour still needs manual confirmation | `docs/analysis/aethersdr-applet-activation.md` | Confirm target macOS AetherSDR binary includes this code path |
| KPA500 uses caret-prefixed read-only commands including `^RVM;`, `^SN;`, `^OS;`, `^WS;`, `^TM;`, `^VI;`, and `^FL;`. | KPA500 Programmer Reference finding supplied before Phase 8 | High | Hardware-verified except `^SN;` still pending transcript evidence | COM21 38400 `^RVM;` -> `^RVM01.54;`, `^OS;` -> `^OS0;`, `^WS;` -> `^WS000 000;`, `^TM;` -> `^TM030;`, `^VI;` -> `^VI689 000;`, `^FL;` -> `^FL00;` | Capture `^SN;` response |
| KPA500 `^VIvvv iii;` reports PA voltage/current in tenths, not AC mains voltage. | KPA500 Programmer Reference | High | Hardware-validated | `^VI689 000;` means `68.9 V`, `0.0 A` | Do not map to PGXL `vac` until a validated PGXL-equivalent field is known |
| KPA500 `^WSppp sss;` reports SWR `000` when no RF is present. | KPA500 Programmer Reference finding supplied before Phase 8 | Medium | Parser tested, hardware response still pending | None yet | Capture no-RF and RF-present transcripts |
| KAT500 sleeps and should be awakened with repeated null `;` commands before queries. | KAT500 Serial Command Reference v2.0 | High | Hardware-validated on COM8 | `; -> ;` at 38400/19200/9600 | Keep configured 38400 unless it fails |
| KAT500 `ST;` must not be used as generic status because it is SWR threshold GET/SET. | KAT500 Serial Command Reference v2.0 | High | Removed from active use | None yet | Probe documented read-only commands individually |
